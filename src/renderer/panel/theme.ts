/**
 * 화면 전체가 공유하는 색과 치수.
 *
 * 예전에는 패널과 설정이 각자 hex 를 들고 있어서, 한 곳의 보라색을 고치면 다른
 * 곳만 옛 색으로 남았다. 값은 여기서만 정한다.
 *
 * 다크 한 벌만 둔 것은 게으름이 아니다 — 이 앱은 투명한 아바타 창 옆에 붙어 사는
 * 상주 프로그램이라 밝은 패널이 화면에서 튄다.
 */
export const T = {
  color: {
    /** 창 바탕. 가장 어둡다. */
    base: '#131120',
    /** 카드·입력칸처럼 한 겹 올라온 면. */
    raised: '#1c1a2b',
    /** 그보다 한 겹 더. 사이드바와 선택된 항목. */
    high: '#252236',
    line: '#2e2b40',
    text: '#e9e7f2',
    /** 설명문. 본문보다 확실히 약해야 훑어볼 때 걸리지 않는다. */
    dim: '#8f8aa6',
    accent: '#6d5ef0',
    accentSoft: '#8b7dff',
    /** 정상 동작 표시. 지금은 대기/작업 중 점 하나에 쓴다. */
    ok: '#4fbf87'
  },
  radius: { md: '10px', lg: '14px', pill: '999px' },
  space: (n: number): string => `${n * 4}px`,
  /** 상주 프로그램이라 본문이 작다. 12~13px 이 창을 좁게 쓰기에 알맞다. */
  font: { body: '13px', small: '12px', title: '15px' }
} as const
